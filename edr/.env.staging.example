# === EDR Stack - Staging Environment Overrides ===
# Copy this file to .env.staging and fill in actual values.

# pgAdmin credentials (staging only — not used in production)
PGADMIN_EMAIL=admin@staging.anyone.local
PGADMIN_PASSWORD=your_secure_pgadmin_password_here
